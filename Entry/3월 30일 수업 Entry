# 엔트리봇 오브젝트의 파이선 코드 / 3월 30일 정보수업 엔트리파이썬 엔트리:https://playentry.org/project/6062ca0e1d9c9700f8deb09a

import Entry

a = 0 #첫 값
b = 0 #두번째 입력값

def when_start():
    Entry.input("첫번째 수 입력")
    a = Entry.answer()
    Entry.input("두번째 수 입력")
    b = Entry.answer()
    if a > b:
        Entry.add_x(a - b)
    else:
        Entry.add_y(b - a)
